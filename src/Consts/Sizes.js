
// Aqui serão colocadas as variaveis que controlaram tamanhos importantes e que
//  São usados constatementes no Jogo
const GameWidth = 1600
const GameHeight = 3536

const mapMainWidth = 1720
const mapMainHeight = 800

const L1MapWidth = 848
const L1MapHeight = 2016

const containerGameWidth = window.innerWidth * .5
const containerGameHeight = window.innerHeight * .6

const DesktopGameWidth = 1720
const DesktopGameHeight = 760

// const mapF1Width = 1720
// const mapF1height = 800

const MobileGameWidth = 368 
const MobileGameHeight = 1920

const ScreenWidth = window.innerWidth - 20
const ScreenHeight =  window.innerHeight - 20

const mapScale = 1.2
const L1MapScale = 1.2
const characterScale = 2.3


export {
    GameWidth,
    GameHeight,
    DesktopGameWidth,
    DesktopGameHeight,
    MobileGameWidth,
    MobileGameHeight,
    ScreenWidth,
    ScreenHeight,
    mapMainWidth,  
    mapMainHeight,  
    containerGameWidth, 
    containerGameHeight,
    mapScale,
    characterScale,
    L1MapWidth,
    L1MapHeight,
    L1MapScale,

    // mapF1Width  
    // mapF1height  
}